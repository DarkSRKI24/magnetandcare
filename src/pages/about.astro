---
import MainLayout from '../layouts/mainLayout.astro';
import { getLangFromUrl, useTranslatedPath } from '../i18n/utils.ts';
import SwiperComponent from '../components/carousel.jsx';

const lang = getLangFromUrl(Astro.url);
const translatePath = useTranslatedPath(lang);
---

<MainLayout lang="sr" title="Magnet And Care - O nama">
    <section class="my-4 md:my-8 mx-4 flex justify-center">
        <div class="w-full md:w-4/5">
            <h2 class="mb-4 md:mb-8 font-bold text-3xl md:text-5xl text-center lg:text-left">Ko smo mi</h2>

            <div class="flex flex-col lg:flex-row gap-5">
                <div class="lg:w-1/2 flex flex-col items-center lg:items-start">
                    <p class="2xl:text-2xl text-center lg:text-justify">
                        Magnet And Care doo je posvećen pružanju profesionalne nege starijim osobama. Takođe, nudimo usluge organizacije u ugostiteljstvu i hotelijerstvu, uključujući radna mesta kao što su konobari, šankeri, sobarice i radnici za peglanje veša. Naš tim stručnjaka je posvećen podršci i organizaciji, pružajući efikasna rešenja za svakodnevne izazove i unapređenje kvaliteta usluga koje nudimo, a istovremeno stvarajući prijatno radno okruženje za sve naše zaposlene. Verujemo da kvalitetna nega dolazi od saosećajnih i posvećenih ljudi, i zato neprestano radimo na izgradnji tima koji deli našu viziju.
                    </p>
                    <p class="my-4 2xl:text-2xl text-center lg:text-justify">
                        Zahvaljujemo vam se što verujete u naš rad i misiju. Ako želite da budete deo našeg tima i doprinesete kroz razne uloge koje nudimo, pozivamo vas da nam se pridružite!
                    </p>
                    <a href={translatePath('/contact/')} class="inline-block px-4 py-2 rounded-lg bg-primary md:hover:bg-accent duration-300 font-bold text-lg text-background md:hover:text-text">Kontaktirajte nas</a>
                </div>
                <div class="w-full lg:w-1/2 flex flex-col m-auto">
                    <div class="rounded-lg overflow-hidden">
                        <SwiperComponent stopAutoplayText="Zaustavite autoplay" startAutoplayText="Pokrenite autoplay" client:load />
                    </div>
                    <button id="autoplay-toggle" class="w-60 self-center mt-2 px-4 py-2 rounded-lg bg-accent font-bold text-lg text-text duration-300 text-nowrap">
                        Zaustavi autoplay
                    </button>
                </div>
            </div>
        </div>
    </section>
</MainLayout>

<style is:global>
.swiper-button-prev::after,
.swiper-button-next::after {
  display: none;
}

.swiper-pagination-bullet {
  height: 10px !important;
  width: 40px !important;
  opacity: 0.6 !important;
  border-radius: 0.5rem !important;
  margin: 0 6px !important;
}

.swiper-pagination-bullet-active {
  opacity: 1 !important;
}

@media screen and (min-width: 640px) {

  .swiper-button-prev::after,
  .swiper-button-next::after {
    display: block;
  }

  .swiper-button-prev,
  .swiper-button-next {
    margin-top: 0 !important;
    transform: translateY(-50%);
    background-color: rgb(255, 255, 255);
    border-radius: 0.5rem;
    padding: 8rem 1.5rem;

    transition: 300ms ease-in-out;
  }

  .swiper-button-prev:hover,
  .swiper-button-next:hover {
    background-color: rgba(255, 255, 255, 0.75);
  }

  .swiper-button-prev {
    left: 20px !important;
  }
  
  .swiper-button-next {
    right: 20px !important;
  }
}
</style>